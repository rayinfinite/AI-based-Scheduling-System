apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: ai-course
    io.kompose.service: pg-data
  name: pg-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
